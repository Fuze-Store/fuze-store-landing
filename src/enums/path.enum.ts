export enum paths {
  home = '/',
  login = '/login',
  register = '/register',
  forgotPassword = '/forgot-password',
  features = '/features',
  aboutUs = '/about-us',
  contactUs = '/contact-us',
  services = '/services',
  pricing = '/pricing',
  account = '/account',
}
